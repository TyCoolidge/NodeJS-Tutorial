const fs = require('fs');
// Can execute node js in the REPL (terminal using 'node', does not save after cltr + 'C')
// nodeJS => PHP => Python

// two params (file, conten)
// fs.writeFileSync('hello.js', "console.log('Hello from Node.js')"); // write file to hard drive (created hello.js file in root directory)
// console.log('Hello from Node.js');
